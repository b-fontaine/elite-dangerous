/// The credit formatter used across the UI.
///
/// Re-exported from `core` so the presentation layer and the roadmap rules
/// produce byte-identical figures.
library;

export '../../../../core/format/credits.dart';
